# Telegram Bot — Документация проекта

## Описание

Telegram-бот для управления заявками на обмен RUB ↔ USDT между партнёрами. Позволяет создавать, публиковать и управлять заявками в нескольких Telegram-группах одновременно, с ролевым доступом, жизненным циклом заявки, авто-истечением, флоу подтверждения сделки и рейтингом партнёров.

---

## Стек и зависимости

- **Python ≥ 3.10** (на проде — Python 3.14, Ubuntu 26.04)
- `python-telegram-bot[job-queue]==22.8` — основная библиотека для работы с Telegram API. `[job-queue]` обязателен: без него не работают TTL, автоудаление и автоподтверждение
- `aiohttp` — HTTP-сервер для healthcheck (слушает только localhost)

Зависимости зафиксированы в `requirements.txt`. (`pytz` не используется — убран.)

---

## Файлы проекта

| Файл | Описание |
|------|----------|
| `simple_bot.py` | Основной файл бота (~4600 строк) |
| `templates_store.py` | Чистая логика именованных шаблонов заявок (CRUD + валидация над dict `state`, без Telegram — тестируется автономно) |
| `tests/test_templates_store.py` | Автономные assert-тесты `templates_store` (запуск: `python3 tests/test_templates_store.py`) |
| `state.json` | База данных (заявки, чаты, роли, пользователи, рейтинги, шаблоны). Создаётся ботом при первом запуске |
| `requirements.txt` | Зависимости Python (PTB 22.8, aiohttp) |
| `.env.example` | Шаблон переменных окружения (копируется в `.env`) |
| `p2p-bot.service` | systemd-юнит для запуска на сервере |
| `DEPLOY.md` | Пошаговая инструкция деплоя на VPS (Ubuntu/Debian) |
| `Claude.md` | Документация проекта (этот файл) |
| `.gitignore` | Исключения для git (`__pycache__/`, `tmp/`, `.env`, `venv/`) |

---

## Git-репозиторий

- **Remote:** `git@github.com:Levonlv/p2p2_new_paris.git` (приватный, доступ по SSH)
- **Ветка:** `main`
- **Игнорируется** (`.gitignore`): `__pycache__/`, `tmp/`, `.env`, `venv/`, `.DS_Store`
- **Коммитится:** код, `state.json` (как бэкап состояния), `.env.example`, доки, `p2p-bot.service`
- ⚠️ Реальный `.env` с `BOT_TOKEN` в репозиторий **не попадает** (в `.gitignore`)
- ⚠️ `Claude.md` содержит IP прод-сервера — при переводе репо в публичный сначала вычистить
- Пуш изменений: `git add -A && git commit -m "..." && git push`

---

## Роли пользователей

| Роль | Возможности |
|------|-------------|
| **Админ** | Полный доступ ко всем чатам и заявкам, управление ролями, бан/разбан |
| **Мерчант** | Создание заявок, отправка только в чаты где состоит |
| **Пользователь** | Просмотр и взятие заявок в работу, история, рейтинг |

Роли задаются через переменные окружения:
- `ADMIN_IDS` — ID администраторов (через запятую)
- `MERCHANT_IDS` — ID мерчантов (через запятую)

---

## Переменные окружения

| Переменная | Описание |
|------------|----------|
| `BOT_TOKEN` | Токен бота от @BotFather |
| `ADMIN_IDS` | ID администраторов через запятую |
| `MERCHANT_IDS` | ID мерчантов через запятую |
| `STATE_FILE` | Путь к файлу состояния (по умолчанию `state.json`) |
| `DEFAULT_TTL_MIN` | Стандартный TTL заявки в минутах (по умолчанию 15) |
| `PORT` | Порт HTTP-сервера (по умолчанию 5000) |
| `HTTP_HOST` | Интерфейс HTTP-сервера (по умолчанию `127.0.0.1` — наружу не торчит) |

---

## Предустановленные чаты (DEFAULT_CHATS)

Все чаты добавляются автоматически при каждом запуске без команды `/register`. **Порядок строк = порядок кнопок** выбора чатов при создании заявки (кнопки строятся из `state["chats"]` в порядке ключей, без сортировки — см. `get_user_chats`):

| Название | ID |
|----------|----|
| OTC | -1003996635324 |
| акулы | -1002271241387 |
| Levon&MP | -4991871961 |
| ZED | -4754013956 |
| 534 Levon Lux | -1003778389969 |
| ving | -4969976093 |
| Levon & GRITHER TEAM | -1002035500063 |
| тесты | -4662200552 |

> ⚠️ Порядок чатов хранится в `state["chats"]` (порядок вставки в dict). Новый чат через `/register` падает **в конец** списка. Изменение порядка/состава в `DEFAULT_CHATS` влияет только на новые/пустые стейты — существующий `state.json` (и прод, и локальный) нужно править отдельно, т.к. `load_state` умеет только **добавлять** предустановленные чаты, но не удалять и не переупорядочивать.

---

## Процесс создания заявки

1. **Направление** — Продам RUB / Куплю RUB
2. **Сумма** — вводится вручную
3. **Банк** — до 2 банков: СБЕР, ТИНЬКОФФ, АЛЬФА, СБП
4. **Платежи** — 1 / до 2 / до 3 платежей
5. **Курс** — Байбит средний / Курс Grinex / Свой курс
6. **Срок (TTL)** — 30 мин / 1 час / 2 часа / 3 часа / В течение дня
7. **Выбор чатов** — один или все доступные

### Быстрое создание по шаблону

Мерчант может сохранить параметры заявки (направление, банк, платежи, курс, TTL —
всё кроме суммы и чатов) как **именованный шаблон** и потом создавать типовые
заявки в 3 шага вместо 7.

- **Сохранить**: на карточке созданной заявки в личке — кнопка **«💾 В шаблон»** →
  ввести имя. Шаблон снимается снимком с самой заявки (поля из `order`), сумма и
  чаты не сохраняются.
- **Использовать**: `/create` при наличии шаблонов показывает их списком
  (+ «➕ Новую с нуля», «✖️ Отмена»). Выбор шаблона → ввод только суммы → выбор
  чатов → публикация.
- **Управление**: команда **`/templates`** — список, переименование (✏️),
  удаление (🗑 с подтверждением).
- **Ограничения**: до **10 шаблонов** на мерчанта, шаблоны приватные (у каждого
  свои). Хранятся в `state["templates"][str(uid)]`. Мерчант без шаблонов видит
  обычный 7-шаговый `/create` без изменений.

---

## Жизненный цикл заявки

```
Создана → Опубликована в чатах
               ↓
         [Взята в работу] ──────────────────────────────────────┐
               ↓                                                  │
    Партнёр нажал «Выполнено»                  Мерчант закрыл сам
               ↓                                       ↓
    Мерчант получает запрос подтверждения    Сделка записана автоматически
               ↓                             Форма оценки мерчанту
    Подтвердил вручную (или авто через 15 мин)        ↓
               ↓                             Заявка удалена из всех чатов
    Мерчант ставит оценку ⭐️ (1–5)
               ↓
    Заявка удалена из всех чатов
               ↓
         [Истекла по TTL]
               ↓
    Переопубликовать / Закрыть → Автоудаление через 5 мин
```

- **Истечение**: по TTL заявка помечается как истёкшая, кнопки удаляются
- **Автоудаление**: если не переопубликована и не закрыта — через 5 минут удаляется из всех чатов
- **Переопубликация**: создаёт новую заявку с теми же параметрами
- **Принудительное закрытие**: если взятая заявка истекла и никто не среагировал — через 10 минут принудительно закрывается

---

## Флоу подтверждения сделки

1. Партнёр нажимает **«✅ Выполнено»** → мерчанту приходит сообщение с кнопками «Подтвердить» / «Оспорить»
2. Через 5 минут — **напоминание** мерчанту
3. Через 15 минут без реакции — **авто-подтверждение** сделки
4. После подтверждения — мерчанту показывается форма **оценки ⭐️ 1–5**
5. Через 15 минут если оценка не поставлена — заявка автоматически закрывается без оценки
6. **Оспорить** — заявка удаляется из всех чатов без записи сделки

**Если мерчант сам закрывает взятую заявку:**
- Сделка записывается автоматически
- Мерчанту показывается форма оценки
- Заявка немедленно удаляется из всех групповых чатов

---

## Система рейтинга партнёров

- Оценки хранятся в `state["ratings"][user_id]`
- Поля: `total_deals`, `total_taken`, `avg_score`, `scores[]`, `avg_completion_minutes`
- Рейтинг виден в карточке партнёра (`/partner @username`)
- Completion rate = `total_deals / total_taken * 100%`
- Среднее время выполнения в минутах

---

## Антиспам система

- Лимит активных заявок на мерчанта
- Лимит напоминаний в час
- Минимальный интервал между напоминаниями
- Логирование подозрительных действий
- Бан/разбан пользователей (`/ban`, `/unban`)
- Просмотр статистики нарушений (`/spamstats`)
- Настройка лимитов через `/setlimits`

---

## Команды бота

### Для всех
| Команда | Описание |
|---------|----------|
| `/start` | Начало работы, краткая справка |
| `/help` | Полный справочник команд |
| `/partner @username` | Карточка партнёра с рейтингом |
| `/history` | История своих сделок |
| `/timezone [смещение]` | Настроить часовой пояс (UTC+N) |

### Для мерчантов и админов
| Команда | Описание |
|---------|----------|
| `/create` | Создать новую заявку (по шаблону или с нуля) |
| `/templates` | Мои шаблоны заявок (список, переименование, удаление) |
| `/myorders` | Список активных заявок |
| `/stats` | Расширенная статистика |

### Только для админов
| Команда | Описание |
|---------|----------|
| `/admins` | Список админов и мерчантов |
| `/merchants` | Список мерчантов |
| `/addmerchant <user_id>` | Добавить мерчанта |
| `/removemerchant <user_id>` | Убрать мерчанта |
| `/register [название]` | Добавить текущий чат |
| `/unregister` | Убрать текущий чат |
| `/list` | Список зарегистрированных чатов |
| `/ban <user_id> <причина>` | Заблокировать пользователя |
| `/unban <user_id>` | Разблокировать пользователя |
| `/spamstats` | Статистика нарушений антиспама |
| `/setlimits <type> <value>` | Настройка лимитов |

---

## Кнопки в группчате (инлайн-клавиатура заявки)

| Кнопка | Кто видит | Действие |
|--------|-----------|----------|
| Взять в работу | Партнёры | Берёт заявку |
| Закрыть заявку | Мерчант/Админ | Закрывает (с авто-подтверждением если взята) |
| Переопубликовать | Мерчант/Админ (истекшая) | Создаёт новую копию |
| Напомнить | Мерчант (истекшая) | Шлёт напоминание в чаты |

### Кнопки в личке партнёра (панель управления)
| Кнопка | Действие |
|--------|----------|
| ✅ Выполнено | Инициирует флоу подтверждения |
| ↩️ Освободить | Освобождает заявку |

---

## Запуск

### Локально
```bash
python3 -m venv venv
./venv/bin/pip install -r requirements.txt
# задать переменные окружения (BOT_TOKEN и т.д.) и запустить:
./venv/bin/python simple_bot.py
```

### Production (текущий деплой)
Бот развёрнут на **VPS 77.110.106.175** (Ubuntu 26.04) как systemd-сервис.

| Параметр | Значение |
|----------|----------|
| Папка | `/opt/p2p-bot/` (юзер `p2pbot`) |
| Окружение | `/opt/p2p-bot/venv` |
| Конфиг | `/opt/p2p-bot/.env` (chmod 600) |
| Сервис | systemd `p2p-bot` (enabled, Restart=always) |
| Бот | @test_p2p_work_bot |
| SSH | только по ключу `~/.ssh/p2p_bot_vps` (пароль отключён) |

```bash
# управление на сервере
systemctl status p2p-bot
systemctl restart p2p-bot
journalctl -u p2p-bot -f
```

Полная инструкция и процедура обновления — в `DEPLOY.md`.

> **Нельзя** запускать локальный бот и прод одновременно (конфликт polling — оба используют один токен).

---

## Важные особенности реализации

- **Все команды только в личке** — бот не отвечает на команды в группах
- **Проверка членства** — мерчанты видят только те чаты, где они состоят
- **Миграция чатов** — когда обычная группа становится супергруппой, у неё меняется chat_id. Все отправки идут через `_record_sent_message`: сохраняет сообщение в `order["messages"]` и, если `msg.chat.id` ≠ запрошенного id (миграция), чинит реестр через `_migrate_chat_id` (переносит запись чата на новый id). Это держит `state["chats"]` и `order["messages"]` в одном id-пространстве — иначе ломается дедупликация отправки (см. ниже)
- **Самолечение мигрировавших групп** — `load_state` при каждой загрузке сверяет чаты с `DEFAULT_CHATS` по имени: если чат с известным именем висит под старым id, переносит его на актуальный id из `DEFAULT_CHATS`. Так прод-`state.json` со старым id чинится автоматически при рестарте
- **Хранение chat_id** — при отправке сообщения в чат всегда сохраняется `msg.chat.id` (реальный ID из ответа Telegram), а не запрошенный ID
- **Дедупликация отправки по чатам** — флоу «отправить в оставшиеся чаты» сравнивает `available_chats` (из `state["chats"]`) с `sent_chats` (id из `order["messages"]`). Если id чата в этих двух источниках расходятся (мигрировавшая группа), кнопка чата не исчезает и заявку можно отправить дважды. Чинится тем, что и реестр, и messages держатся на одном (новом) id — см. «Миграция чатов»
- **Единая функция удаления** — `_close_order_from_chats` используется везде: удаляет из групчатов, из личного чата бота (`bot_message_id`), удаляет заявку из стейта
- **Дедупликация чатов** — при загрузке проверяется название чата, чтобы не дублировать мигрировавшие группы
- **Нельзя запускать** локальный бот и деплой одновременно (конфликт polling)
- **`bot_message_id` / `bot_chat_id`** — ID сообщения заявки в личном диалоге с ботом, хранится в заявке для корректного удаления
- **Атомарное сохранение стейта** — `save_state` пишет во временный файл + `fsync` + `os.replace`; `state.json` не бьётся при краше/ребуте во время записи
- **HTTP-сервер только на localhost** — слушает `127.0.0.1` (env `HTTP_HOST`), эндпоинт `/test/list_chats` со списком чатов наружу не торчит
- **Файл бота** ранее назывался `simple_bot (1).py` (с пробелом) — переименован в `simple_bot.py`

---

## Ключевые функции

| Функция | Описание |
|---------|----------|
| `_close_order_from_chats` | Удаляет заявку из всех чатов + бота, убирает из стейта |
| `_do_confirm_deal` | Ядро подтверждения сделки: запись, оценка, уведомления |
| `_auto_confirm_job` | Авто-подтверждение через 15 мин если мерчант не среагировал |
| `_auto_close_confirmed_order_job` | Авто-закрытие через 15 мин если мерчант не поставил оценку |
| `_force_close_claimed_expired_job` | Принудительное закрытие взятой истёкшей заявки через 10 мин |
| `edit_message_safe` | Редактирование с обработкой ChatMigrated и BadRequest |
| `send_message_safe` | Отправка с обработкой ChatMigrated |
| `_record_sent_message` | Сохраняет отправленное сообщение в `order["messages"]`; при миграции группы чинит реестр чатов и возвращает реальный chat_id |
| `_migrate_chat_id` | Переносит запись чата в `state["chats"]` со старого id на новый (миграция группы → супергруппа) |
| `build_keyboard` | Строит инлайн-клавиатуру в зависимости от роли и статуса заявки |
| `render_message` | Формирует текст заявки для отображения |
| `record_deal` | Записывает сделку в историю партнёра |
| `record_rating` | Записывает оценку партнёру |
